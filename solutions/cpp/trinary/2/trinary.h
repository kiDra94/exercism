#if !defined(TRINARY_H)
#define TRINARY_H
#include <string>

namespace trinary {

int to_decimal(std::string number);
bool is_valid(std::string number);

}  // namespace trinary

#endif  // TRINARY_H
